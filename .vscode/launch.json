{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug NES Emulator",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/target/debug/nes_emulator",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "cargo build"
    }
  ]
}
